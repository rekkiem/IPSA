# IPSA Agent v2.1 — Dependencias Python
# ──────────────────────────────────────

# Core (obligatorias)
yfinance>=0.2.40
pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0

# Cache local (obligatoria para price_cache.py)
pyarrow>=14.0.0

# ML (opcional — Extension 2)
xgboost>=2.0.0
scikit-learn>=1.3.0

# Tests
pytest>=7.0.0

# Opcional: cargar .env automáticamente
# python-dotenv>=1.0.0
